// FaceTrack AI — App root (Phase 1).
// Routing lives in routes/AppRoutes. Providers are set up in main.jsx.

import AppRoutes from './routes/AppRoutes';

export default function App() {
  return <AppRoutes />;
}
