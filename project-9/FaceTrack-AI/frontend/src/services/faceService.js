// FaceTrack AI — Face service (Phase 3 placeholder).
// Face registration/approval (`faceData`) + match scoring. Not in Phase 1.

export const FACE_DATA = 'faceData';

// TODO(Phase 3): implement face registration, approval, and matching.
